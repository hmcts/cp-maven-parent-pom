name: Scanning
on:
  pull_request:
    branches:
      - '**'
  schedule:
    - cron: '0 4 * * 4' # Every Thursday at 04:00
  workflow_dispatch:

jobs:
  scan:
    uses: hmcts/cp-maven-parent-pom/.github/workflows/shared-secret-scanning.yml@validate-shared-workflow
    secrets: inherit